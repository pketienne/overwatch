# Allow the desktop user to drive the overwatch instance services without
# a password. Wildcards cover all instance names since the same desktop user
# owns every instance shortcut on this host.
<%= @target_user %> ALL=(ALL) NOPASSWD: \
    /usr/bin/systemctl start overwatch@*.service, \
    /usr/bin/systemctl stop overwatch@*.service
